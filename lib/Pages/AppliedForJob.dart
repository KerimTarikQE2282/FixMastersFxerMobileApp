import 'package:flutter/material.dart';

class AppliedForJob extends StatelessWidget {
  const AppliedForJob({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
