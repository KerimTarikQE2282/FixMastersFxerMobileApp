import 'package:flutter/material.dart';

class DetailedJobView extends StatelessWidget {
  const DetailedJobView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
