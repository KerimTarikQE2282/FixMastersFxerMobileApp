import 'package:flutter/material.dart';

class SelectedForJob extends StatelessWidget {
  const SelectedForJob({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
